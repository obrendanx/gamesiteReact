import React, { Component } from 'react';

const LgInfoBox = (props) => {
    return(
        <div className="LgInfoBox">
            <h1>This is component {props.compNum}</h1>
        </div>
    );
}
export default LgInfoBox;
